# HFC 线上 503 只读排查摘要（脱敏）

时间：2026-07-10 14:25–14:27 CST
范围：独立生产链路，只读健康检查、容器状态和聚合日志/数据库核对。

## 结论

本轮本地开发、测试和候选镜像没有部署到生产，不能解释这次 503。聚合证据显示，503 集中来自单一客户端直接请求尚未向该分组公开的 `gpt-5.6-terra`；该分组当时没有 Terra exact mapping，因此调度返回 `no available accounts`。

## 线上状态

- Nginx、Sub2API、PostgreSQL、Redis、model-cache 均 healthy。
- 本地与公网健康检查均 HTTP 200。
- 没有 OOM、容器重启或资源耗尽证据。
- 同类 503 在排查窗口后停止，未形成全站宕机。

## 处置边界

- 没有停止、重启、reload 或替换生产服务。
- 没有修改生产数据库、账号池、API Key、分组、渠道、费率或路由。
- 没有读取客户 prompt、密钥正文或账号凭据。

## 后续要求

在逐档 entitlement、exact mapping、计费预检和 canary 完成前，不应让客户直接使用未公开的 `gpt-5.6-*` ID；客户端应切回该分组已公开且有账号映射的模型。
